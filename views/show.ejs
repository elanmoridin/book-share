<!DOCTYPE html>
<html>
    <head>
        <link rel = "stylesheet" href="/main.css">
        <link rel = "icon" type="image/png" href="/book_icon.png">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
        <link href="https://fonts.googleapis.com/css2?family=Gentium+Book+Basic&display=swap" rel="stylesheet">
        <title>CBC</title>
    </head>
    <body>
        <%- include('./partials/nav.ejs')%>
        <h1>Community Collection of Books<img src="/title_book.jpg"  width="100"></h1>
        <div class = "text-center">
        <h1>Book Information</h1>
        <img src="<%=books.img%>" width="300px">
        <br>
        Book Title: <%=books.title %>
        <br>
        Book Author: <%=books.author %>
        <br>
        Book Genre: <%=books.genre %>
        <br>
        <form action="/<%=books.id%>/checkout?_method=PUT" method="POST">
            <% if(books.checkedOut == false) {%>
                <input type="submit" value="Checkout Book" class="btn btn-lg btn-primary">
            <% } else { %>
                <p>Book is checked out by <%=books.checkedUser%></p>
                <input type="submit" value="Check in Book" class="btn btn-lg btn-primary">
                <% } %>
        </form>
        <form action="/<%= books.id %>?_method=DELETE" method="POST">
            <a href="/<%=books.id%>/edit" class="btn btn-sm btn-success">Edit Book</a>
            <input type="submit" value="Delete Book" class="btn btn-danger btn-sm"/>
        </form>
        </div>
        <nav>
            <a class="btn btn-info" href="/">Back to Community Book Shelf</a>
        </nav>
    </body>
</html>